spring.application.name=Medicos-Service
server.port=8083

# --- Base de datos ---
spring.datasource.url=jdbc:postgresql://localhost:5432/clinica_medicos
spring.datasource.username=postgres
spring.datasource.password=post
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Seguridad JWT ---
jwt.secret=clave-super-secreta-compartida

# --- si esta en true todo se permite, si esta en false regresa la seguridad ---
